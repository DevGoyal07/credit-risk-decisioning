# --- Core ---
pandas>=2.0
numpy>=1.26
pyarrow>=15.0          # fast parquet I/O (the Amex CSVs are huge; convert to parquet once)

# --- Model ---
lightgbm>=4.3          # GBM — matches Amex's real production stack
scikit-learn>=1.4      # CV, calibration, metrics

# --- Explainability / reason codes ---
shap>=0.45             # per-applicant adverse-action reason codes

# --- Plots for the eval report ---
matplotlib>=3.8
